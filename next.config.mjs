/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/my-next-weather-app',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
